function MainLogin({ children }) {
    return (
        <main className="my-5 md:bg-white md:w-[30rem] md:mx-auto md:rounded-xl">
            {children}
        </main>
    )
}

export default MainLogin
