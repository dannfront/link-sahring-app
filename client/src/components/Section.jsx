function Section({children}) {
    return (
        <section className="container bg-white mt-5 rounded-xl p-2 md:px-10 lg:w-[45rem] lg:m-0">
            {children}
        </section>
    )
}

export default Section
